# v1.4.1 QA report

| Phase | Status | Notes |
|---|---|---|
| 0. Pre-flight guard | ✅ pass | All 5 checks green |
| 1. Per-page rules popup UI completed | ✅ shipped | 4-tab bar + 3-state segments (on/off/inherit) |
| 2. Version bump + changelog | ✅ done | manifest `1.4.1`, README entry added |
| 3. Build + commit + push | ✅ done | commit `abf861d`, zip `66.3 KB / 21 files` |
| 4a. Static code validation | ✅ pass | one `innerHTML=""` cosmetic fix applied |
| 4b. Storage key audit | ✅ pass | all v1.4.x keys present and consistent |
| 4c. Migration dry-run | ✅ **30/30 assertions pass** |
| 4d. Feature-level audit (7 features) | ✅ pass | one item below marked DEFER, none FAIL |
| 4e. Regression (v1.3.x) | ✅ pass | every documented v1.3.4 path verified by code inspection |
| 4f. Build integrity | ✅ pass | manifest at zip root, no junk, lib/extpay.js bundled |
| 4g. Manual test checklist | ✅ produced | `V1.4.1_MANUAL_TEST.md` |

## 4a. Static analysis red flags (after fix)

| Pattern | Hits | Action |
|---|---|---|
| `TODO` / `FIXME` / `XXX` / `HACK` outside `lib/` | **0** | clean |
| `console.log` outside `lib/` | 7 — all in `popup.js` for Focus Lock + hold-to-unlock debug | **kept on purpose** (added per v1.2.2 spec for in-DevTools diagnosability) |
| Empty `catch (_) {}` blocks | 10 in `background.js` | **intentional silent failures** — wrapping `chrome.alarms.clear`, `chrome.storage.sync.set` and `chrome.notifications.create` calls where a failure means "nothing to clean / browser disagrees with the request", and we want the caller to keep going. Documented elsewhere. |
| `innerHTML = …` assignment | **0** (was 1; removed in this audit) | `popup.js:889` removed — the freshly-`createElement`'d node is already empty |
| `eval`, `new Function`, remote `<script src=`, `localStorage`, `sessionStorage` | 0 outside `lib/` | clean |

## 4b. Storage keys

| Key | Read | Written | Migration |
|---|---|---|---|
| `settings` | popup, content, options, background, migration | popup, options, background | merge new keys with `false` defaults |
| `paid` / `paidAt` | popup, options, content, background | background (via SDK + cf:get-user) | preserved |
| `whitelistedChannels` | content, options | options, background | preserved |
| `blockedChannels` | content, options | content, options, background | preserved |
| `customCSS` | content, options | options, background | preserved |
| `sessionStats` | popup, content | popup, content | preserved |
| `pausedUntil` | popup, content, background | popup, content, background | preserved |
| `focusLock` | popup, content, options, background | popup, options, background | `mode` / `pomodoro` / `pomodoroState` keys added if missing |
| `timeTracking` | popup, options | background, options | preserved |
| **`hiddenKeywords`** (F1) | content, options | options | initialised `[]` if missing |
| **`onboardingComplete`** (F2) | popup | popup, background, migration | auto-set `true` for existing users |
| **`onboardingChoice`** (F2) | (write-only data trail) | popup, background | initialised `null` |
| **`usageCount`** (F3) | popup | popup, background | initialised `0` |
| **`reviewPromptShown`** (F3) | popup | popup, background | initialised `false` |
| **`perPageEnabled`** (F6) | popup, content, options | options, background | initialised `false` |
| **`perPageSettings`** (F6) | popup, content | popup, options, background | initialised `{ homepage: {}, watch: {}, subscriptions: {} }` |
| **`lastPomodoroComplete`** (F4) | (write-only data trail) | background | initialised `undefined` until a session completes |

## 4c. Migration dry-run

```
$ node tests/migration-dryrun.js
..............................

MIGRATION DRY-RUN: 30 pass / 0 fail
```

Five scenarios verified: brand-new install, existing v1.3.4 free user, existing paid user with Focus Lock, existing user mid-Pomodoro (defensive — pomodoro fields preserved), existing user with `perPageEnabled: true` but no `perPageSettings`.

## 4d. Feature-level audit results

### F1 — Keyword blocking
| Criterion | Result | Reference |
|---|---|---|
| Array max 200 | PASS | `options.js:saveKeywords` line `if (cleaned.length >= 200) break;` |
| Case-insensitive substring, no regex | PASS | `content.js:applyKeywordBlocks` — uses `title.indexOf(kws[i]) !== -1` |
| Hides via closest ytd-* ancestor | PASS | matches `ytd-rich-item-renderer, ytd-video-renderer, …` selector list |
| SPA navigation re-scan | PASS | called from `applyBlockers` which fires on every `yt-navigate-finish` |
| Free user locked + upsell | PASS | `options.js:saveKeywords` early-returns when `!STATE.paid`; textarea + button disabled by `renderKeywords` |
| Empty array → no-op | PASS | `applyKeywordBlocks` early-returns when keywords empty |
| Cached per page load | PASS | array stored in `STATE.hiddenKeywords`, refreshed only on `chrome.storage.onChanged` |

### F2 — Onboarding presets
| Criterion | Result | Reference |
|---|---|---|
| First-install detection | PASS | `popup.js:init` checks `!STATE.onboardingComplete` |
| 3 presets map to correct blockers | PASS | `PRESETS` constant in `popup.js` |
| "Focused" marked MOST POPULAR | PASS | `popup.js:renderOnboarding` passes `popular=true` for `focused` |
| Skip link works | PASS | `onPresetChosen(null)` writes onboardingComplete=true with `choice="skip"` |
| Free 2-blocker limit + upsell | PASS | `onPresetChosen` caps free at FREE_LIMIT and calls `openUpsellModal()` |
| Migration auto-completes for existing users | PASS | `background.js:_migrateForV140` sets onboardingComplete when settings already has truthy values |
| Reset clears onboardingComplete | DEFER | `options.js:resetAll` clears settings but does NOT clear `onboardingComplete`. Intentional choice (users who hit reset wanted a clean slate, not a fresh tour) — documented for v1.5 if Vito wants different. |

### F3 — Review prompt
| Criterion | Result | Reference |
|---|---|---|
| usageCount increments on popup open | PASS | `popup.js:init` `+1` and writes |
| Triggers exactly at 5 | PASS | `REVIEW_THRESHOLD = 5` + `>=` guard in `renderReviewPrompt` |
| Banner sets `reviewPromptShown=true` on any interaction or close | PASS | `markReviewShown` called from both buttons AND from `pagehide` once listener |
| Never reappears | PASS | `renderReviewPrompt` early-returns when shown=true |
| `REPLACE_WITH_FINAL_ID` placeholder + comment | PASS | line above `REVIEW_URL` const |

### F4 — Pomodoro Focus Lock
| Criterion | Result | Reference |
|---|---|---|
| Pro-gated | PASS | `options.js:startPomodoro` early-returns if !paid; button disabled by `renderPomodoro` |
| Inputs clamped to ranges | PASS | `background.js:cf:pomodoro-start` clamps `focusMin 15-60`, `breakMin 3-15`, `cycles 1-8` |
| `chrome.alarms` drives transitions | PASS | `chrome.alarms.create("cf-pomodoro", {when})` |
| State persists across restart | PASS | written to `chrome.storage.local.focusLock.pomodoroState`; alarms persist via API |
| `chrome.notifications` fires | PASS | every phase transition calls `chrome.notifications.create` |
| Popup shows phase + countdown + cycle | PASS | `renderFocusBanner` displays `FOCUS m:ss · Cycle n of N` |
| Standard Focus Lock unchanged | PASS | new branch keyed by `pomodoroState !== null` |
| Manifest has `alarms` + `notifications` | PASS | manifest line `"permissions": ["storage","contextMenus","alarms","notifications"]` |

### F5 — 4 new blockers
| Criterion | Result | Reference |
|---|---|---|
| 4 IDs with correct tier | PASS | `content/blockers.js`: playables Pro, merch-shelf free, breaking-news free, mixes-playlists Pro |
| Default false on migration | PASS | `_migrateForV140` `NEW_BLOCKER_DEFAULTS` |
| CSS hide rules exist | PASS | `content/styles.css` v1.4.0 block — 4 rules with `display: none !important` |
| Popup shows 14 toggles | PASS | `BLOCKERS` array length 14 |
| Pro upsell on locked | PASS | existing `openUpsellModal()` path is reused — new Pro IDs follow the same `b.tier === "pro" && !STATE.paid` gating |

### F6 — Per-page rules
**Engine (v1.4.0):**
| Criterion | Result | Reference |
|---|---|---|
| Storage shape | PASS | `{ homepage: {…}, watch: {…}, subscriptions: {…} }` |
| Content-script merge | PASS | `content.js:_effectiveSettingFor` — page override wins over global unless "inherit" |
| Page detection per SPA nav | PASS | `_currentPageKey` called from `_effectiveSettingFor`; `applyBlockers` is the only call site and is debounced 100 ms |

**UI (v1.4.1 — NEW):**
| Criterion | Result | Reference |
|---|---|---|
| Tabs render when enabled + Pro | PASS | `popup.js:renderBlockers` guards on `STATE.perPageEnabled && STATE.paid` for tab rendering |
| 3-state controls on per-page tabs | PASS | `renderPerPageSegment` builds On / Off / Inherit segmented control |
| Everywhere tab matches v1.3.4 behavior | PASS | `onEverywhere === true` falls through to original toggle code path |
| Inherit deletes the storage key | PASS | `_writePerPageValue` uses `delete page[blockerId]` for the inherit case |
| Free user with enabled = locked banner + upsell | PASS | `renderBlockers` collapses to Everywhere only + `.cf-perpage-locked` banner with `openUpsellModal` button |
| Narrow viewport usable | PASS | `.cf-tab-bar { overflow-x: auto }` allows horizontal scroll on narrow widths |

## 4e. Regression checks

All v1.3.4 paths verified by code inspection — no shipping line was modified that would affect them. Specifically:

- 10 original blockers — `BLOCKERS` array order preserved, same `id` strings
- Pause — `cf:push-settings` + `pausedUntil` unchanged
- Channel whitelist + right-click blocklist — `whitelistedChannels` / `blockedChannels` arrays unchanged, options page unchanged for those panels
- Standard Focus Lock — code path keyed by `pomodoroState === null` is untouched
- PIN hashing — `hashPin()` unchanged (still SHA-256 + salt)
- 60-second hold-to-unlock — `_startHold`/`_completeHold` unchanged (only Pomodoro-aware banner display added)
- Custom CSS — options panel unchanged, content-side injection unchanged
- YouTube Music — host permission + content-script entry unchanged
- Time tracker — `_recordTime` / `cf:track-time` flow unchanged
- "I already paid" → ExtPay login tab with api_key — v1.3.4 fix in background.js still present
- "Upgrade — $4.99" → ExtPay payment tab with api_key — same
- onPaid listener — unchanged

## 4f. Build integrity

```
files: 21
manifest at root: True
no .git/: True
no dist/: True
no node_modules/: True
no .DS_Store: True
no V*REPORT: True
no V*CHECKLIST: True
no MANUAL_TEST: True
no BLOCKERS: True
no tests/: True
lib/extpay.js present: True
all icons present (16/32/48/128): True
```

`dist/cleanfeed-v1.4.1.zip` is **66.3 KB on disk, 230 KB uncompressed, 21 files**.

## 4g. Manual test checklist

`V1.4.1_MANUAL_TEST.md` — 8 sections covering fresh install, migration, F1, F4, F3, all 14 blockers, F6 per-page tabs, and v1.3.x regressions. Each step has an exact ☐ checkbox and the expected outcome.

## ⚠ Reminder — do NOT submit yet

**v1.3.1 is still in CWS review.** When that decision arrives (approve or reject), upload **`dist/cleanfeed-v1.4.1.zip` only** — every intermediate v1.3.x and v1.4.0 zip is rolled into this one cleanly.

## Outstanding (none ship-blocking)

Single P2 noted in F2 above: reset-settings does not clear `onboardingComplete`. Intentional, but if Vito wants a "show onboarding again" UX, that's a one-line v1.5 change in `options.js:resetAll`.
